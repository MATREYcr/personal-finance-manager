Single-line text field for forms — amounts, dates, currency codes, notes.

```jsx
<Label>Amount</Label>
<Input type="number" placeholder="0.00" />
```

Focus state shows a 3px accent ring (see `--shadow-focus-ring`). Disabled state drops to the sunken surface at 60% opacity.
