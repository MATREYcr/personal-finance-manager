Base content container — dashboard summary tiles, form panels, settings section.

```jsx
<Card>
  <CardHeader>
    <CardTitle>Income</CardTitle>
  </CardHeader>
  <CardContent>
    <span className="amount">3,200.00 USD</span>
  </CardContent>
</Card>
```

1px border + `--shadow-xs` only — no heavier card shadows in this system.
